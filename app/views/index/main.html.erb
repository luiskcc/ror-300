<div class="container mx-auto px-4 py-8">
  <h1 class="font-bold text-5xl text-center mb-12 text-gray-800">Ruby on Rails 227 Q&A</h1>
  <p class="text-center text-lg text-gray-600 mb-12 max-w-3xl mx-auto">
    Dive into our comprehensive collection of 227 questions and answers, organized by topic to help you master Ruby on Rails.
  </p>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    <% @sections.each do |section| %>
      <%= link_to section_path(section), class: "bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-2xl hover:-translate-y-2 transition-all duration-300" do %>
        <div class="p-6">
          <h2 class="font-bold text-2xl mb-2 text-gray-900"><%= section.name %></h2>
          <p class="text-gray-600"><%= section.description %></p>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
